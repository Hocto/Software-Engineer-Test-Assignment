plugins {
    // Lets Gradle auto-download the Java 25 toolchain (Amazon Corretto) when it is not
    // already installed, so the build works on a reviewer's machine with no PATH changes.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'core-banking'
