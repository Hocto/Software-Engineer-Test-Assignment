<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tuum.banking.repository.TransactionMapper">

    <resultMap id="transactionResultMap" type="com.tuum.banking.model.entity.Transaction">
        <id property="id" column="id"/>
        <result property="accountId" column="account_id"/>
        <result property="amount" column="amount"/>
        <result property="currency" column="currency"/>
        <result property="direction" column="direction"/>
        <result property="description" column="description"/>
        <result property="balanceAfter" column="balance_after"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.tuum.banking.model.entity.Transaction"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO transaction (account_id, amount, currency, direction, description, balance_after)
        VALUES (#{accountId}, #{amount}, #{currency}, #{direction}, #{description}, #{balanceAfter})
    </insert>

    <select id="findByAccountId" resultMap="transactionResultMap">
        SELECT id, account_id, amount, currency, direction, description, balance_after, created_at
        FROM transaction
        WHERE account_id = #{accountId}
        ORDER BY id
    </select>

</mapper>
